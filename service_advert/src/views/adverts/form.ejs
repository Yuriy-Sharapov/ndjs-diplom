<!-- <form method="POST" class="mt-5"> -->
<form method="POST" class="mt-5" enctype="multipart/form-data">
    <div class="form-group">
        <label for="inputShortText">Заголовок объявления</label>
        <input type="text"
               value="<%= advert.shortText %>"
               name="shortText"
               class="form-control"
               id="inputShortText">
    </div>

    <div class="form-group">
        <label for="inputDesc">Описание</label>
        <textarea
                rows="3"
                name="description"
                class="form-control"
                id="inputDesc"
        ><%= advert.description %></textarea>
    </div>

    <!-- <% if(advert.images && advert.images.length) { %>
        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
            
            <div class="carousel-inner">
                <% advert.images.map((el, idx) => {  %>
                    <div class="carousel-item active">
                        <img class="d-block w-100" src="<%= el %>" alt="First slide">
                    </div>
                <% }) %>
            </div>

            <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Предыдущая</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Следующая</span>
            </a>
        </div>
    <% } %> -->

    <div class="form-group">
        <label for='file'>Выберите изображения:</label>
        <input type='file' multiple='multiple' accept='image/*' name='uploadedImages' id='file' />
        <span class='hint'>Возможные форматы изображений: jpg, jpeg, png.</span>
        <!-- <button type='submit'>upload</button>        -->
        
        <!-- <label for="InputImageUrls">Путь к изображениям</label>
        <input type="text"
               value="<%= advert.images %>"
               name="images"
               class="form-control"
               id="InputImageUrls"
               disabled>

        <label for="uCoverPath">Загрузить обложку</label>
        <input type="file" name="cover" class="form-control" id="uCoverPath"> -->
    </div>

    <div class="form-group">
        <label for="inputTags">Теги</label>
        <textarea
                rows="3"
                name="tags"
                class="form-control"
                id="inputTags"
        ><%= advert.tags %></textarea>
    </div>

    <div class="form-group">
        <input type="checkbox"
               name="isDeleted"
               id="inputIsDeleted"               
               <%= advert.isDeleted ? "checked" : "unchecked" %>
               >
        <label for="inputIsDeleted">Скрыть объявление</label>
    </div>

    <div class="form-group text-right ">
        <button type="submit" class="btn btn-primary">Сохранить</button>
    </div>
    
</form>